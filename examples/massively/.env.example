# Wild & Still — environment configuration
# Copy to .env and adjust values before running.

# Server port
PORT=8080

# Application mode: debug | release
MODE=debug

# Admin credentials (change these before deploying!)
ADMIN_USER=admin
ADMIN_PASS=changeme
